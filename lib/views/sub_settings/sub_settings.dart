import 'package:flutter/material.dart';

class SubSettingsView extends StatelessWidget {
  const SubSettingsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Column(
        children: [
          Text('SubSettings VIEW'),
          // SizedBox(height: 12),
/*          ElevatedButton(
            onPressed: () {},
            child: const Text("Navigate to HOME CHILD OF HOME VIEW"),
          ),*/
        ],
      ),
    );
  }
}
